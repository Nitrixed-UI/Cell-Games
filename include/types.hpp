#include <cstdint>
#include <vector>
#include <string>

using u8 = uint8_t;
using u16 = uint16_t;
using u32 = uint32_t;
using u64 = uint64_t;

using i8 = int8_t;
using i16 = int16_t;
using i32 = int32_t;
using i64 = int64_t;

using f32 = float;
using f64 = double;

using u64_vec = std::vector<u64>;
using u32_vec = std::vector<u32>;
using u16_vec = std::vector<u16>;
using u8_vec = std::vector<u8>;

using i64_vec = std::vector<i64>;
using i32_vec = std::vector<i32>;
using i16_vec = std::vector<i16>;
using i8_vec = std::vector<i8>;

using f64_vec = std::vector<f64>;
using f32_vec = std::vector<f32>;

using str_vec = std::vector<std::string>;